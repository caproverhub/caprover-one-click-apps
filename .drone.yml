---
kind: pipeline
type: docker
name: default

steps:
- name: validate
  when:
    event:
    - pull_request
  image: node:14-alpine
  commands:
  - yarn global add pnpm
  - pnpm i
  - pnpm run validate_app
  - pnpm run formatter
